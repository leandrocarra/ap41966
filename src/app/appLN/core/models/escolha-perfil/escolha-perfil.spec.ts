import { EscolhaPerfil, SubPerfilRural } from './escolha-perfil';

describe(EscolhaPerfil.name, () => {
  it('should create an instance, quando escolhido EscolhaPerfil', () => {
    expect(new EscolhaPerfil()).toBeTruthy();
  });


describe(SubPerfilRural.name,()=>{
  it('deve se criar uma instancia, quando escolhido SubPerfilRural', () => {
    expect(new SubPerfilRural() ).toBeTruthy();
  });

})
  
});

describe(SubPerfilRural.name, () => {
  it('should create an instance, quando escolhido SubPerfilRural', () => {
    expect(new SubPerfilRural()).toBeTruthy();
  });
});
