import { TestBed } from '@angular/core/testing';
import { MatDialogModule, MatDialogRef, MAT_DIALOG_DATA } from '@angular/material/dialog';
import { DialogClassePrincipalService } from './dialog-classe-principal.service';


// describe(DialogClassePrincipalService.name, () => {
//   let service: DialogClassePrincipalService;

//   beforeEach(() => {
//     TestBed.configureTestingModule({
//       providers: [DialogClassePrincipalService],
//       imports: [
//         MatDialogModule,
//         MatDialogRef,
//       ]
//     });
//     service = TestBed.inject(DialogClassePrincipalService);
//   });

//   // it('should be created', () => {
//   //   expect(service).toBeTruthy();
//   // });
// });
