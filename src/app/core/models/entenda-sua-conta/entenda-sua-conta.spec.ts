import { EntendaSuaConta } from './entenda-sua-conta';

describe(EntendaSuaConta.name, () => {
  it('should create an instance', () => {
    expect(new EntendaSuaConta()).toBeTruthy();
  });
});
