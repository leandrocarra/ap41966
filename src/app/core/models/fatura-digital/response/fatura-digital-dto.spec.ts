import { FaturaDigitalDTOResponse } from "./fatura-digital-dto";

describe (FaturaDigitalDTOResponse.name, () => {
    it('should create an instance', () => {
        expect(new FaturaDigitalDTOResponse())
    });
});