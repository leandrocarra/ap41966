import { DebitoAutomatico } from './debito-automatico';

describe('DebitoAutomatico', () => {
  it('should create an instance', () => {
    expect(new DebitoAutomatico()).toBeTruthy();
  });
});
